// Need to use the React-specific entry point to import createApi
import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react';


// Define a service using a base URL and expected endpoints
export const <%= props.name %>Api = createApi({
  reducerPath: '<%= props.name.toLowerCase() %>',
  baseQuery: fetchBaseQuery({ baseUrl: 'ADD_YOUR_URL_HERE' }),
  endpoints: (builder) => ({
    get<%= props.name %>: builder.query({ query: (name) => `ADD_ENDPOINT_HERE` }),
  }),
});

// Export hooks for usage in functional components, which are
// auto-generated based on the defined endpoints
export const { useGet<%= props.name %> } = <%= props.name %>Api;
